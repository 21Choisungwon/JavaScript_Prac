if 문

    if(조건식){
        코드1;
        코드2;
        코드3;
        ...;
    }
    

    if문 바깥 코드;

    if문 바깥 코드;

    if문을 만나면 조건식을 확인한다
    1-1.조건식이 참이면 if문 안쪽으로 들어와서 if문 안쪽 코드를 차례로 실행
    1-2.조건시기 거짓이라면 if문 안쪽으로 들어가지않는다.


if ~else (if문)
코드:
    if(조건식){
        조건식이 참일떄 실행할코드;
        조건식이 참일떄 실행할코드;
        조건식이 참일떄 실행할코드;
        ...;
    }
    else{
        조건식이 거짓일때 실행할 코드;
        조건식이 거짓일때 실행할 코드;
        조건식이 거짓일때 실행할 코드;
        ...;
    }
    코드;

    if문 조건식이 거짓일 때 무언가 실행시키고 싶은 코드가 있다면 else 를 작성한다

    if ~ else if ~ else
        if(조건식 1){
            조건식 1이 참일때 실행되는 영역;
        }
        else if (조건식 2){
            조건식1은 거짓이고 조건식2가 참일때 실행되는 영역;
        }
        else if(조건식 3){
            조건식 1,2거짓 3이 참일때 실행되는 영역;
        }
        ...{
            ...;
        }
        else{
            위에있는 조건식이 모두 거짓일 때 실행되는 영역;
        }


switch 문
    조건에 따라서 특정 코드를 실행시키고자 할 때 사용한다
    switch(표현식){
        case 값1:
            표현식 === 값1 일때 들어와서 실행되는 코드;

        case 값2:
            표현식 === 값2 일때 들어와서 실행되는 코드;
        
        case 값3:
            표현식 === 값3 일때 들어와서 실행되는 코드;
       
        case 값4:
            표현식 === 값4 일때 들어와서 실행되는 코드;
        default:
            표현식이 위에있는 모든 경우에 해당하지 않을 때 실행되는 코드;
            
    }